{
  "name": "@shopit/api-gateway",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@shopit/api-gateway:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@shopit/api-gateway:build:development"
          },
          "production": {
            "buildTarget": "@shopit/api-gateway:build:production"
          }
        }
      }
    }
  }
}
